rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read on all images inside the listings folder
    // Allow writes only by authenticated users to any path under listings/
    match /listings/{allPaths=**} {
      allow read;
      allow write: if request.auth != null;
    }
  }
}
